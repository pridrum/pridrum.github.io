# Venue styling for publication badges on /publications/.
#
# Deliberately empty. _layouts/bib.liquid styles a badge only when the entry's
# abbr matches a key here; with no match it uses the theme's default badge,
# which is how this site has always looked. Adding keys with a `color` here
# overrides that and repaints the badges.
#
# The physics venues that used to be in this file (AJP, PhysRev, Vision) were
# al-folio demo data and matched none of Priscila's abbreviations, which is why
# they never had any visible effect.
{}
